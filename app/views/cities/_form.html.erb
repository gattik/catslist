<%= simple_form_for @city do |f| %>

  <%= f.input :name, label: "Your City's name, please" %>
  <%= f.input :subdomain, label: "What do you want this city's URL to start with?" %>
  <%= f.submit "submit", class: "btn btn-primary" %>

<% end %>
