<h1><%= @city.name %></h1>

<% @category.each do |category| %>
  <div class="category">
     <h2><%= link_to category.name, category_path(category) %></h2>
<% end %>



<br>
<%= button_to "New Post", new_post_path(@post), class: "btn btn-primary btn-lg" %> <br>

<%= button_to "All Cities", root_url(subdomain: false), method: :get, class:  "btn btn-default" %> 
